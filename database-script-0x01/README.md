# Database Schema (DDL)

This folder contains the **schema.sql** file that defines the Airbnb-style database schema.

- `schema.sql`: SQL commands to create tables, relationships, and indexes.
- Entities: User, Property, Booking, Payment, Review, Message
- Relationships enforced with **foreign keys** and **constraints**.

